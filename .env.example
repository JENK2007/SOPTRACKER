# Server Port
PORT=3005

# NOTE:
# Per security policy, user emails, passwords, and Teams webhooks are NOT stored here in .env.
# Users enter their email ID, password, and Teams Webhook directly in the Web Application UI (Settings page).
# Credentials are sent per-request over the secure local connection.
